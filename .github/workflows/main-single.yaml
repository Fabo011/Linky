name: Build Linky-Cloud

on:
  push:
    branches: [ "master" ]

jobs:
  build:
    runs-on: ubuntu-latest

    strategy:
      matrix:
        node-version: [18.x]

    steps:
      - uses: actions/checkout@v3

      - name: Use Node.js ${{ matrix.node-version }}
        uses: actions/setup-node@v3
        with:
          node-version: ${{ matrix.node-version }}
          cache: 'npm'

      # QEMU for multi-arch builds - REQUIRED for arm64 on x86 runners
      - name: Set up QEMU
        uses: docker/setup-qemu-action@v3

      # Docker Buildx for multi-platform support
      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - run: npm ci
      - run: npm run build

      - name: Get package version
        id: version
        run: echo ::set-output name=version::$(node -pe "require('./package.json').version") 

      - name: Log in to GitHub Container Registry
        uses: docker/login-action@v2
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.PIPE_TOKEN }}

      # Single-arch build and push
      - name: Build and Push Multi-Arch Docker image
        run: |
          IMAGE_ID=ghcr.io/${{ github.repository }}
          VERSION=${{ steps.version.outputs.version }}
          IMAGE_ID=$(echo $IMAGE_ID | tr '[:upper:]' '[:lower:]')

          docker buildx build \
            --platform linux/arm64 \
            --build-arg VUE_APP_SUPABASE_URL="${{ secrets.VUE_APP_SUPABASE_URL }}" \
            --build-arg VUE_APP_API_KEY="${{ secrets.VUE_APP_API_KEY }}" \
            -t $IMAGE_ID:$VERSION \
            -t $IMAGE_ID:latest \
            --push .
